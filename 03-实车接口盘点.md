# 03｜实车接口盘点

状态：**容器 1 与 App 桥接接口已完成只读盘点；容器 2按计划暂缓**  
检查时间：2026-07-13  
目标地址：10.224.104.165  
原则：盘点过程只读取系统、容器、ROS 和网络服务状态，不发送实车运动命令。

## 1. 连通性结果

| 项目 | 结果 | App 用途 |
| --- | --- | --- |
| SSH TCP 22 | 可连接 | 运维与只读盘点，不由 App 使用 |
| AppBridge WebSocket TCP 9092 | 可连接 | App 状态、服务请求和遥控指令 |
| Flask/MJPEG TCP 6500 | 可访问 | 独立遥控视频；当前阶段暂不接入 |
| 历史 TCP 6000 | 仅绑定旧地址 192.168.1.11 | 当前 App 不使用 |

登录密码不得写入本文、Git 仓库、脚本或 App。

## 2. 容器结果

| 角色 | 容器名称 | 容器 ID | 镜像 | 当前盘点状态 |
| --- | --- | --- | --- | --- |
| 导航、建图、底盘基础 | nifty_dirac | 81c99e0c3f98 | yahboomtechnology/ros-foxy:5.0.1 | 已运行并完成接口盘点 |
| iCar 避障、跟随、视觉 | sharp_maxwell | 4eb3445dd740 | icar/ros-foxy:1.0.2 | 已停止；按计划暂缓 |

容器 1 已确认环境：

- ROS 2：Foxy
- ROS_DOMAIN_ID：30
- ROBOT_TYPE：x3
- RPLIDAR_TYPE：a1
- CAMERA_TYPE：astraplus

## 3. 容器 1 的 ROS 接口

### 3.1 关键话题

| 话题 | 类型 | App 用途 |
| --- | --- | --- |
| /cmd_vel | geometry_msgs/msg/Twist | 独立遥控、停车 |
| /map | nav_msgs/msg/OccupancyGrid | 地图显示、建图 |
| /scan | sensor_msgs/msg/LaserScan | 雷达显示、定位与导航 |
| /odom | nav_msgs/msg/Odometry | 小车位姿和速度 |
| /odom_raw | nav_msgs/msg/Odometry | 底盘原始里程计 |
| /tf | tf2_msgs/msg/TFMessage | map→odom→底盘的动态坐标变换 |
| /tf_static | tf2_msgs/msg/TFMessage | base_link→laser 等静态坐标变换 |
| /voltage | std_msgs/msg/Float32 | 电压/电量状态 |
| /diagnostics | diagnostic_msgs/msg/DiagnosticArray | 设备与节点诊断 |
| /app/runtime_state | App 运行状态消息 | 当前模式、阶段、就绪与故障 |

### 3.2 App 服务

| WebSocket command | ROS 服务 | 作用 |
| --- | --- | --- |
| start_mapping | /app/start_mapping | 开始建图 |
| stop_mapping | /app/stop_mapping | 结束建图 |
| save_map | /app/save_map | 保存地图 |
| load_saved_map | /app/load_saved_map | 加载已保存地图 |
| start_navigation_base | /app/start_navigation_base | 只启动底盘/雷达基础 |
| start_navigation_dwa | /app/start_navigation_dwa | 启动 DWA 导航 |
| start_navigation_teb | /app/start_navigation_teb | 启动 TEB 导航 |
| cancel_navigation | /app/cancel_navigation | 取消当前导航目标 |
| stop_navigation | /app/stop_navigation | 停止导航相关进程 |
| emergency_stop | /app/emergency_stop | 停止当前运动 |

### 3.3 Action

| Action | App 用途 |
| --- | --- |
| /navigate_to_pose | 设置单个导航目标 |

### 3.4 已观察到的主要节点

容器 1 当前可见 Nav2 节点，以及 driver_node、sllidar_node、robot_app_api 和 app_bridge。节点会随建图、导航或底盘基础模式切换而变化，因此 App 应以运行状态接口为准，不只依赖固定节点名单。

课程手册 6.1 的 m1 启动截图进一步确认，单次执行 m1 会启动 Joint/Robot State、X3 底盘驱动与基础节点、IMU 滤波、EKF、`yahboom_joy_X3`、`sllidar_node`、静态 TF 和 `slam_gmapping`。这说明 m1 在当前课程镜像中是整套建图 bringup，而不是仅启动一个 GMapping 节点。

## 4. AppBridge WebSocket 协议

连接地址：

~~~text
ws://10.224.104.165:9092
~~~

协议版本：1。WebSocket 同时承载 JSON 文本帧和 `ICAR` 二进制帧，Android 必须分别实现文本回调与 `ByteString` 回调，不能把二进制帧交给 JSON 解析器。

实车连接已观察到 `hello/state/runtime` 文本帧；下述二进制字段来自当前 AppBridge 源码。类型 1～3 仍需在 m1 实车运行时完成抓包和画面验收，确认 Jetson 部署版本与本记录一致。

### 4.1 JSON 文本帧

连接后可能收到的消息顺序不固定，App 必须按 op 字段独立处理：

~~~json
{"op":"hello","protocol":1}
{"op":"state","runtime":{...}}
{"op":"runtime","mode":"navigation","phase":"awaiting_initial_pose","ready":false}
~~~

速度控制：

~~~json
{
  "id": "请求唯一标识",
  "command": "twist",
  "linear_x": 0.2,
  "linear_y": 0.0,
  "angular_z": 0.0
}
~~~

服务请求：

~~~json
{
  "id": "请求唯一标识",
  "command": "start_navigation_base"
}
~~~

服务响应：

~~~json
{
  "op": "response",
  "id": "与请求相同的标识",
  "ok": true,
  "message": "执行结果"
}
~~~

`ok: true` 的含义是对应 ROS 服务已经受理请求，并不统一代表异步工作已经完成：

- `start_mapping` 返回成功后，仍须等待 `op=runtime` 的 `mode=mapping`、`phase=ready`、`ready=true`。
- `save_map` 返回成功后，目前只表示 m4 保存进程已经启动；桥接服务没有等待进程退出，也没有核验 PGM/YAML 文件，因此 UI 只能显示“保存命令已受理”，不能直接显示“保存成功”。

### 4.2 二进制帧公共格式

- 字节序：网络字节序/大端（big-endian）。
- 字节 0～3：ASCII `ICAR`，十六进制为 `49 43 41 52`。
- 字节 4：无符号 8 位包类型。
- 当前 SLAM 页面使用类型 1（地图）、2（位姿）、3（雷达）。类型 4～7 预留给导航代价地图和路径，不属于本次建图验收范围。

| 类型 | 来源 | 最大发送频率 | 用途 |
| --- | --- | --- | --- |
| 1 | /map | 1 Hz | 占据栅格地图 |
| 2 | /tf、/tf_static、/odom | 10 Hz | map 坐标系中的小车 x/y/yaw |
| 3 | /scan | 5 Hz | 降采样激光距离 |

### 4.3 类型 1｜地图帧

帧头固定为 33 字节，对应 `!4sBIIfffII`：

| 偏移 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| 0 | 4 bytes | magic | `ICAR` |
| 4 | uint8 | packet_type | 固定为 1 |
| 5 | uint32 | width | 栅格宽度 |
| 9 | uint32 | height | 栅格高度 |
| 13 | float32 | resolution | 米/像素 |
| 17 | float32 | origin_x | 地图原点 x，单位米 |
| 21 | float32 | origin_y | 地图原点 y，单位米 |
| 25 | uint32 | raw_length | 解压长度，必须等于 `width × height` |
| 29 | uint32 | compressed_length | 后续 zlib 数据长度 |
| 33 | bytes | compressed_cells | zlib 压缩的栅格字节 |

解压后每格是 ROS `OccupancyGrid.data` 的有符号 8 位值：`-1` 未知、`0` 空闲、`100` 占用，1～99 为中间概率；传输时 `-1` 表示为无符号字节 255。栅格按 ROS 行优先顺序排列，Android 绘制到屏幕时需要翻转 Y 轴。协议 v1 未携带地图原点 yaw 和 frame_id，当前实车应以 map 坐标系且原点 yaw 为 0 的地图验收。

### 4.4 类型 2｜小车位姿帧

总长固定为 17 字节，对应 `!4sBfff`：

| 偏移 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| 0 | 4 bytes | magic | `ICAR` |
| 4 | uint8 | packet_type | 固定为 2 |
| 5 | float32 | x | map 坐标系 x，单位米 |
| 9 | float32 | y | map 坐标系 y，单位米 |
| 13 | float32 | yaw | 航向角，单位弧度，逆时针为正 |

桥接服务优先读取 `map→base_link/base_footprint`，或组合 `map→odom` 与 `odom→base_link/base_footprint`；只有还未获得 map 位姿时才临时使用 `/odom`。Android 应将该位姿通过地图 `origin` 和 `resolution` 转成画布坐标。

### 4.5 类型 3｜雷达帧

帧头固定为 15 字节，对应 `!4sBHff`，随后为 `sample_count` 个 float32：

| 偏移 | 类型 | 字段 | 说明 |
| --- | --- | --- | --- |
| 0 | 4 bytes | magic | `ICAR` |
| 4 | uint8 | packet_type | 固定为 3 |
| 5 | uint16 | sample_count | 采样数，当前最多 180 |
| 7 | float32 | angle_min | 第一个采样角，单位弧度 |
| 11 | float32 | angle_increment | 降采样后的有效角度增量 |
| 15 | float32[] | ranges | 每个采样的距离，单位米 |

桥接服务按原始 `/scan` 长度等间隔降采样；非有限值转换为 `0.0`。Android 应忽略小于等于 0 的点。协议 v1 不携带 `range_min`、`range_max`、frame_id 或 `base_link→laser` 外参，因此雷达叠加主要用于实时观察；精确叠加必须结合实车已确认的静态 TF。

## 5. SLAM 建图结论、建议流程与验收

课程手册《6.1 激光雷达 SLAM 建图实战实验》和 m1 启动截图已经确认：当前课程镜像执行 m1 后会同时启动 X3 底盘驱动、Robot/Joint State、IMU 滤波与 EKF、`yahboom_joy_X3`、A1 雷达、静态 TF 和 `slam_gmapping`。因此：

- 建图模式直接启动 m1，不要先启动或并行启动 n1。n1 属于导航基础链路，和 m1 重复启动底盘、雷达、TF 会有串口、设备或节点冲突风险。
- m2 只在 Jetson/虚拟机桌面启动 RViz2。Android 不启动 RViz2，而是通过 9092 的类型 1～3 二进制帧原生显示地图、小车位姿和雷达，这就是 App 对 m2 的替代。
- App 摇杆替代终端 m3，二者不能同时运行。App 在摇杆按住/偏离中心时以 10 Hz 发布 `/cmd_vel`，松手回中、离开页面、App 进后台、断线、急停或停止建图时先发零速度。
- m4 固定覆盖 `/root/yahboomcar_ros2_ws/yahboomcar_ws/src/yahboomcar_nav/maps/yahboomcar.pgm` 和同目录的 `yahboomcar.yaml`。保存期间必须保持 m1 和 `/map` 运行，先停车、后保存、确认后再停止 m1。

建议状态流程：

~~~text
已连接 9092
  → 确认当前非导航模式并发送零速度
  → start_mapping（启动 m1；不要启动 n1）
  → 等待 mapping/starting
  → waiting_node → waiting_scan → waiting_tf → waiting_map
  → mapping/ready（此时才启用建图摇杆）
  → 原生显示地图、位姿、雷达；摇杆 10 Hz 发布 Twist
  → 保存前摇杆回中并发送零速度
  → save_map（m4，覆盖固定文件名）
  → UI 显示“保存命令已受理”，需要额外证据才能显示“保存成功”
  → 再次发送零速度
  → stop_mapping
  → 等待 runtime 回到 idle
~~~

建图就绪由 Jetson 当前监督逻辑共同判定：检测到 `slam_gmapping`、最近 2.5 秒内有 `/scan`、存在 `base_link→laser` 静态 TF，并且 m1 启动后已经收到新的 `/map`。启动超过 45 秒仍不满足条件时会进入 `failed`，App 应展示 `detail` 和 `error`，不能继续开放摇杆。

验收标准：

1. 连接后收到 `hello/protocol=1`，文本帧和二进制帧均能独立解析，畸形或截断帧不会导致 App 崩溃。
2. 点击开始建图只触发 m1，不触发 n1/m2/m3；运行状态完整进入 `mapping/ready` 后才允许移动。
3. 地图帧通过 zlib 校验并满足 `raw_length=width×height`；未知、空闲、占用栅格颜色不同，地图 Y 轴方向、比例和原点正确。
4. 位姿标记随小车运动更新且航向正确；雷达采样数不超过 180，0 值不绘制。
5. 摇杆持续操作时 `/cmd_vel` 约 10 Hz；松手、回中、切页、后台、断线、急停和停止建图时，下一控制周期内发布零 Twist 并停止非零循环。
6. App 摇杆与终端 m3、实体 Joy/`joy_ctrl`、Nav2 不同时输出速度；任何模式切换先停车。
7. 保存按钮需要覆盖确认；收到 `ok=true` 只显示“保存命令已受理”。现阶段“保存成功”必须另行核验容器内两个文件均存在、可读取且修改时间已更新。
8. 保存完成后再停止建图；最终 runtime 为 `idle`、`slam_gmapping` 停止，且小车保持零速度。

## 6. 独立遥控结论

当前独立遥控使用容器 1，不等待容器 2。独立遥控不应为了运动而启动 m1；它只要求 X3 底盘驱动已由独立遥控基础流程（例如 `start_navigation_base`/n1）启动并订阅 `/cmd_vel`。用户观察到“启动 m1 后摇杆才可用”的原因正是 m1 在当前课程镜像中也包含底盘驱动，但 m1 应只用于 SLAM 建图模式。

2026-07-13 实车只读复查确认：

- driver_node 正在运行，并订阅 /cmd_vel。
- /cmd_vel 当前有 app_bridge、robot_app_api、joy_ctrl 三个发布者。
- 空闲观察期间 /cmd_vel 没有持续消息，暂未发现其他发布者连续覆盖 App 指令。
- AppBridge 可以直接把 twist 命令发布到 /cmd_vel。

当前 App 采用与 m3 相同的 ROS Twist 控制思路：

~~~text
连接 AppBridge 9092
  → 用户启用遥控
  → 按住期间以 10 Hz 持续发布 Twist
  → 松手发布零速度并停止循环
~~~

遥控输出限制：

- linear_x：-0.20～0.20
- linear_y：-0.20～0.20
- angular_z：-1.0～1.0
- 按住按钮或摇杆时每 100 ms 发送一次，松手立即发送零速度。
- App 进入后台、断开连接、停用遥控或触发全局急停时发送零速度。
- App 独立遥控、m3 键盘遥控和 Nav2 自动导航不得同时输出速度。

课程材料中的鸿蒙示例是另一条控制链路：它使用 TCP 6000 和 `$...#` 编码帧。实车的 6000 当前只绑定旧地址 192.168.1.11，因此本阶段 Android 不使用该端口，仍使用 ROS/AppBridge 9092。

## 7. 视频接口（已确认，暂缓接入）

| 项目 | 结果 |
| --- | --- |
| 页面 | http://10.224.104.165:6500/index2 |
| 视频流 | /video_feed |
| 协议 | HTTP MJPEG multipart |
| 使用位置 | 仅独立遥控页 |
| 当前计划 | 暂不实现，先完成无视频独立遥控 |

## 8. 后续待盘点

1. 容器 2 的避障、跟随、警卫和视觉追踪启动/停止接口。
2. /voltage 到百分比电量的标定规则。
3. diagnostics 中需要向用户展示的故障项和中文说明。
4. m4 保存完成通知、退出码和地图文件自动校验；当前响应仅表示命令受理。

容器快捷命令和 Launch 源码另见 04-容器快捷命令与Launch源码.md。
