# 02｜功能范围与模式定义

状态：**已确认，作为 V2 的开发依据**

## 目标

V2 只控制一台实体智能小车。该车在不同容器/ROS 环境中提供导航与 iCar 智能行为功能；App 需要统一展示这些能力，但任何时刻只能运行一种运动模式。

## 功能范围

| 功能 | 所属模式 | 用户可见能力 | 验收结果 |
| --- | --- | --- | --- |
| 独立视频遥控 | 独立遥控 | 视频画面、摇杆/方向控制、停车、急停 | 视频稳定；按住移动、松手停车 |
| 地图与建图 | 建图 | 地图、雷达、小车位姿、开始/保存/结束建图 | 可保存并读取可用于导航的地图 |
| 自动导航 | 导航 | DWA/TEB、初始位姿、目标点、取消、人工接管 | 小车到达目标或可安全取消 |
| 雷达避障 | 智能行为 | 启动、运行状态、停止、急停 | 小车自主避开障碍物 |
| 雷达跟随 | 智能行为 | 启动、运行状态、停止、急停 | 小车按设定距离跟随目标 |
| 雷达警卫 | 智能行为 | 启动、运行状态、停止、急停 | 小车执行警卫行为并显示状态 |
| 视觉追踪 | 智能行为 | 相机状态、启动、运行状态、停止、急停 | 小车按视觉目标运动 |
| 设备状态 | 全局 | 电量、速度、雷达、相机、底盘故障、当前模式 | 离线/故障有明确提示 |

## 页面结构

```text
顶部：连接状态 + 当前模式 + 全局急停
├─ 独立遥控：视频 + 手动控制
├─ 地图与建图：地图显示 + 建图任务
├─ 自动导航：定位 + 目标点 + 导航状态 + 人工接管
├─ 智能行为：避障 / 跟随 / 警卫 / 视觉追踪
└─ 设备状态：电量、速度、传感器、故障详情
```

## 互斥状态机

| 当前模式 | 可进入方式 | 切换前的强制动作 |
| --- | --- | --- |
| 空闲 | 选择任一功能 | 确认无速度输出 |
| 独立遥控 | 空闲；导航人工接管 | 停止其他所有模式；确认视频在线 |
| 建图 | 空闲 | 停止导航和智能行为 |
| 导航 | 空闲；建图完成 | 已保存地图；建图已结束；停止智能行为 |
| 避障 / 跟随 / 警卫 / 视觉追踪 | 空闲 | 停止独立遥控、建图、导航与其他智能行为 |

### 人工接管

导航人工接管采用固定顺序：

```text
取消 Nav2 目标 → 发布零速度 → 确认导航不再输出速度 → 进入独立遥控
```

这保证手动遥控和 Nav2 不会并发控制同一个 `/cmd_vel`。

### 紧急停止

紧急停止只停止小车运动：停止当前运动行为并发布零速度。它不停止 Docker 容器，也不触发蜂鸣器。

## 命名规则

课程中导航容器和 iCar 容器都使用 `m1`、`n1` 等快捷命令，但含义不同。V2 界面禁止直接以快捷命令为主要按钮名称，统一使用：

- “开始建图（导航模式）”
- “启动 DWA 导航（导航模式）”
- “启动雷达避障（智能行为）”
- “启动视觉追踪（智能行为）”

具体 ROS 服务、话题与视频地址必须以 `03-实车接口盘点.md` 的实测结果为准。
