# 05｜自动导航实现与验收

状态：**Android V2 与 Jetson 补丁已在本地实现；尚不等于实车验收通过**  
记录时间：2026-07-13  
适用设备：当前唯一一台 Yahboom/iCar 实体小车  
适用容器：容器 1（nifty_dirac，ROS 2 Foxy）

本文记录自动导航第一版的功能边界、9092 协议、安全顺序和实车验收项目。所有“已实现”均指本地源码与静态/单元测试范围；只有在补丁部署到 Jetson、ROS 运行链路正常并在安全场地完成测试后，才可标记为“实车已验收”。

## 1. 功能范围与启动顺序

课程/RViz 的人工操作顺序是：

~~~text
n1 启动底盘、雷达与 TF
  → n2 启动 RViz2 导航显示
  → n3（DWA）或 n4（TEB）启动 Nav2，二选一
  → 设置初始位姿
  → 下发导航目标
~~~

Android V2 不启动或嵌入 RViz2，对应顺序是：

~~~text
连接 ws://<Jetson-IP>:9092
  → App 原生地图页请求 load_saved_map
  → 收到保存地图的 type 1 数据并核对地图
  → start_navigation_base 启动 n1
  → 等待 runtime=navigation_base/ready
  → start_navigation_dwa 或 start_navigation_teb
  → runtime=navigation/awaiting_initial_pose 时发布 initial_pose
  → 等待 runtime=navigation/ready
  → 发布单点目标，或由 App 顺序派发最多 20 个航点
  → 取消、人工接管或停止时执行零速度保护
~~~

`n3` 与 `n4` 都会启动一套 Nav2，必须互斥。切换算法前必须没有活动目标；后端停止原算法后才能启动另一算法。建图 `m1` 与导航 `n1/n3/n4` 也必须互斥。

## 2. 9092 JSON 命令

所有控制请求均为 WebSocket JSON 文本帧，包含唯一 `id`。桥接层返回：

~~~json
{"op":"response","id":"同一请求 id","ok":true,"message":"处理结果"}
~~~

服务型命令只需要 `id` 和 `command`：

| command | 对应 ROS 接口 | 用途 |
| --- | --- | --- |
| `load_saved_map` | `/app/load_saved_map` | 临时读取并发布 `maps/yahboomcar.yaml`，供 App 在启动 Nav2 前查看 |
| `start_navigation_base` | `/app/start_navigation_base` | 启动 n1，建立底盘、雷达、里程计与 TF 基础 |
| `start_navigation_dwa` | `/app/start_navigation_dwa` | 启动 n3/DWA Nav2 |
| `start_navigation_teb` | `/app/start_navigation_teb` | 启动 n4/TEB Nav2 |
| `cancel_navigation` | `/app/cancel_navigation` | 请求取消当前 NavigateToPose 目标 |
| `stop_navigation` | `/app/stop_navigation` | 停车并结束 n3/n4 与 n1 环境 |
| `emergency_stop` | `/app/emergency_stop` | 发布零速度并取消当前目标，不负责蜂鸣器或其他硬件动作 |

示例：

~~~json
{"id":"uuid","command":"start_navigation_base"}
{"id":"uuid","command":"start_navigation_dwa"}
{"id":"uuid","command":"cancel_navigation"}
~~~

位姿型命令使用 map 坐标系，`x`、`y` 单位为米，`yaw` 单位为弧度：

~~~json
{"id":"uuid","command":"initial_pose","x":1.2,"y":-0.4,"yaw":1.57}
{"id":"uuid","command":"goal_pose","x":3.0,"y":2.1,"yaw":0.0}
~~~

`initial_pose` 发布到 `/initialpose`；`goal_pose` 发布到 `/app/goal_pose`，再由后端送入 Nav2 `NavigateToPose` Action。响应“已发布”只证明桥接层已接收并发布消息，不代表定位已收敛、目标已被 Nav2 接受或导航已经完成。

## 3. Runtime 阶段与 UI 放行条件

AppBridge 转发 `/app/runtime_state` 为 JSON 文本帧：

~~~json
{"op":"runtime","mode":"navigation","phase":"awaiting_initial_pose","ready":false,"detail":"...","error":""}
~~~

### 3.1 n1 基础阶段

~~~text
navigation_base/starting
  → waiting_odom
  → waiting_scan
  → waiting_tf
  → navigation_base/ready
~~~

任一步骤启动失败或超过后端超时限制进入 `failed`。只有 `navigation_base/ready` 才允许启动 n3/n4。

### 3.2 Nav2 阶段

~~~text
navigation/starting
  → waiting_nodes
  → waiting_localizer
  → awaiting_initial_pose（ready=false，但允许发布初始位姿）
  → waiting_local_costmap
  → waiting_global_costmap
  → waiting_action
  → navigation/ready
~~~

`ready=false` 不等于所有操作都被禁止。`awaiting_initial_pose` 的职责就是等待用户发布初始位姿；App 也允许在 `waiting_local_costmap`、`waiting_global_costmap`、`waiting_action` 和 `ready` 阶段重新发布初始位姿。只有到 `navigation/ready` 且 `navigation_ready=true` 后，才允许发布导航目标。

后端就绪检查必须确认：Nav2 核心节点存在、AMCL 确实订阅 `/initialpose`、局部/全局代价地图数据新鲜，并且 NavigateToPose Action Server 可用。不能把监督节点自身对 `/initialpose` 的订阅误判为 AMCL 已就绪。

## 4. 二进制 type 4–7 导航协议

所有二进制帧均使用网络字节序/大端，公共前缀为 ASCII `ICAR` 加 1 字节包类型。type 1–3 的地图、位姿、雷达格式见 03、04 文档。

| 类型 | ROS 数据 | 布局 | 频率上限 | App 显示 |
| --- | --- | --- | --- | --- |
| 4 | `/local_costmap/costmap` | `!4sBIIfffII` + zlib cells | 5 Hz | 局部代价地图 |
| 5 | `/global_costmap/costmap` | `!4sBIIfffII` + zlib cells | 1 Hz | 全局代价地图 |
| 6 | `/plan` | `!4sBH` + `count × !fff` | 5 Hz | 全局路径 |
| 7 | `/local_plan` | `!4sBH` + `count × !fff` | 5 Hz | 局部路径 |

type 4/5 与 type 1 使用相同栅格元数据：`width`、`height`、`resolution`、`origin_x`、`origin_y`、原始长度、压缩长度，随后是 zlib 压缩的有符号占据值。type 6/7 的每个点是 map 坐标系的 `x`、`y`、`yaw` 三个 float32；后端最多下采样为 512 点。Android 最多仅把这些数据用于显示，Nav2 的规划与避障必须完全在 Jetson 本地闭环，不能依赖手机连接。

## 5. 单点与多航点派发

### 5.1 单点导航

用户在原生地图上选择目标位置并调整朝向。App 仅在 `navigation/ready`、没有活动路线且目标有效时发送一个 `goal_pose`。后端收到目标后应依次报告“等待接受”“已接受/导航中”和最终结果。

ROS 2 Action 常用最终状态码：

| 状态码 | 含义 | App 行为 |
| --- | --- | --- |
| 4 | SUCCEEDED | 单点完成；多航点才可派发下一点 |
| 5 | CANCELED | 取消完成；可结束路线或解锁人工接管 |
| 6 | ABORTED | 导航失败；停止路线，不自动跳到下一点 |

### 5.2 多航点导航

多航点由 Android App 顺序派发，不要求后端新增 FollowWaypoints 接口：

1. 用户在地图上逐点加入列表，每条路线最多 20 个航点。
2. App 发送第 1 个 `goal_pose`，等待 Nav2 最终结果。
3. 只有收到该目标状态码 4 后，App 才等待短暂间隔并发送下一点。
4. 收到状态码 5、状态码 6、目标拒绝、连接断开或用户取消时立即终止剩余列表。
5. 任一时刻只允许一个活动目标，不能一次并发发送 20 个目标。

## 6. 取消、人工接管与停止顺序

### 6.1 普通取消

App 先终止本地多航点派发并发送一次全零 `/cmd_vel`，再调用 `cancel_navigation`。后端的“取消请求已发送/已受理”只是中间状态，不能立即视为目标已取消；应等待 NavigateToPose 最终状态码 5。

### 6.2 人工接管

导航期间允许人工接管，但自动控制与人工控制不能同时输出：

~~~text
锁定遥控
  → 立即发送零速度
  → cancel_navigation
  → 每 100 ms 持续发送零速度
  → 等待当前目标最终状态码 5
  → 再发送一次零速度
  → 解锁 App 遥控
~~~

若取消失败、返回状态码 6、连接断开或等待超时，人工遥控必须保持锁定，不能假定 Nav2 已停止输出。若当时没有活动目标，也要先成功发送零速度后才能启用人工接管。

### 6.3 停止导航环境

安全停止顺序固定为：

1. App 停止本地航点队列并发送零速度。
2. 后端再次发布零速度。
3. 取消活动 NavigateToPose Action。
4. 停止 n3 或 n4 启动的 Nav2 栈。
5. 停止 n1 底盘/雷达基础环境。
6. 发布 `idle/idle/ready=false`，App 清除导航活动状态和导航叠加层。

WebSocket 最后一个客户端断开时，桥接层也必须从 ROS 执行线程发布零速度，避免网络中断后保留旧速度。

## 7. 后端阻断 Bug 与部署要求

旧版 `app_bridge.py` 的 `publish_grid_if_due(message, packet_type, ...)` 虽然接收 `packet_type`，但封包头却硬编码为 `PACKET_MAP`（type 1）。结果是局部/全局代价地图被错误标记成主地图：

- Android 收不到真正的 type 4/5；
- 代价地图可能覆盖 App 中的保存地图；
- 导航叠加与协议验收无法成立。

这是自动导航原生可视化的阻断 Bug。V2 补丁把封包头改为传入的 `packet_type`，部署后必须抓包确认 type 4 与 type 5 均真实出现。若 Jetson 仍运行旧版脚本，即使 Android APK 已构建成功，也不能宣称导航功能完成。

同一后端补丁还需保证：取消请求等待最终 Action 结果、停止导航先发零速度、AMCL 订阅检查不被监督节点自身误判、旧目标迟到回调不能清除新目标状态，以及最后一个 WebSocket 客户端断开时发送零速度。

## 8. 实车验收清单

以下项目必须在地图已备份、底盘架空或有足够安全空间、旁边有人可立即物理断电/停车的条件下逐项执行。测试时不得同时运行 m1、m3、另一套 Nav2 或其他 `/cmd_vel` 发布者。

### 8.1 部署与静态核对

- [ ] Jetson 已备份旧版 `app_bridge.py` 和 `robot_app_api.py`。
- [ ] V2 补丁已部署，Python 语法检查和静态安全脚本通过。
- [ ] 9092、API 节点和所需 `/app/*` 服务存在；尚未启动运动功能时不产生非零速度。
- [ ] `maps/yahboomcar.yaml` 与其 PGM 文件可读、比例/原点合理。
- [ ] 抓包确认 type 4/5 不是 type 1，type 6/7 的点数与包长度一致。

### 8.2 启动与定位

- [ ] `load_saved_map` 后 App 能显示正确地图，方向、比例和原点与 RViz 一致。
- [ ] n1 依次通过 odom、scan、TF 检查并进入 `navigation_base/ready`。
- [ ] 仅启动 DWA 或 TEB 其中一个；进程和 ROS 节点无重复。
- [ ] Nav2 到 `awaiting_initial_pose` 时 `ready=false`，但初始位姿按钮可用。
- [ ] 发布初始位姿后 AMCL 位姿与实车朝向一致，随后进入 `navigation/ready`。
- [ ] 局部/全局代价地图、局部/全局路径能在 Android 原生地图正确对齐。

### 8.3 导航行为

- [ ] 单个近距离目标被接受，反馈剩余距离，最终状态码 4，停车位置与朝向合理。
- [ ] 障碍物由 Jetson/Nav2 本地处理；断开手机显示不会让规划闭环依赖手机。
- [ ] DWA 与 TEB 分别单独验收；切换时旧算法与旧目标已停止。
- [ ] 2–3 个安全航点按顺序执行，前一点状态码 4 后才发送下一点。
- [ ] 20 个航点上限有效，第 21 个不能加入。
- [ ] 某航点状态码 6 或被拒绝时，后续航点不再发送。

### 8.4 安全行为

- [ ] 普通取消先停车，并在最终状态码 5 后显示“已取消”。
- [ ] 人工接管等待期间连续观测到零速度；状态码 5 前遥控保持锁定。
- [ ] 取消失败、超时或状态码 6 时，人工接管仍保持锁定。
- [ ] 停止导航严格按“零速度 → 取消目标 → 停 n3/n4 → 停 n1”执行。
- [ ] App 切页、退后台、主动断开、网络中断和最后客户端断开均不会遗留非零 `/cmd_vel`。
- [ ] 全局紧急停止只执行停车/取消，不触发蜂鸣器、容器停止或其他未授权硬件动作。

## 9. 当前结论

本地 Android 导航页面、二进制解码、状态机和 Jetson 修复补丁构成了可部署的第一版实现；本地构建、单元测试或静态脚本通过，只能证明源码一致性。尚未完成上述实车清单前，项目状态必须保持“本地实现完成，实车待验收”，不能写成“自动导航已经在实车通过”。
